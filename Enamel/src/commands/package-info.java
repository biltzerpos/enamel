/**
 * Contains all the abstract and concrete commands used by the GUI. There exists
 * exactly one command for each of the command sections in the official Player
 * documentation.
 */
package commands;